<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.erasmusvalencia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DefaultTheme">
        <activity android:name=".BaseThemeChangerActivity"></activity>
        <activity android:name=".BaseRecyclerActivity" />
        <activity
            android:name=".InfoActivity"
            android:label="Informations"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PlacesActivity"
            android:label="Places"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EventsActivity"
            android:icon="@drawable/ic_event_note_black_24dp"
            android:label="Events"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EventDetailsActivity"
            android:label="Details"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Erasmus app">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>